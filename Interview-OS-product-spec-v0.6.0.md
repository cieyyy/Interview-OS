# Interview OS v0.6.0 产品与架构说明

> 产品定位：面向求职者的个人职业 AI 操作系统。v0.6.0 基于 v0.5.0 增量升级，不删除既有功能、数据、接口或工作流。

## 1. 版本目标与边界

v0.6.0 将原有的求职工具集合组织为一条可追溯的职业成长闭环：

```text
发现岗位 -> 分析 JD -> 匹配能力 -> 修改简历 -> 包装项目
-> AI 面试训练 -> 知识沉淀 -> 持续学习 -> 获得 Offer
```

本版本完成产品导航、设计系统、AI 会话模型、知识空间和无损迁移的升级。真实招聘平台抓取、自动沟通与自动投递不属于本版本强制交付；已有浏览器可见岗位 Bridge、连接器契约、筛选与投递辅助框架继续保留。

## 2. 信息架构

主导航统一为 12 个模块：

| 模块 | 主要职责 | v0.6.0 状态 |
|---|---|---|
| 工作台 | 今日任务、训练、待优化回答、技能缺口和目标岗位 | 已实现 |
| AI 职业教练 | 六种训练模式和统一会话上下文 | 已实现 |
| 简历工坊 | 基于 JD、项目和技能证据维护定向版本 | 保留并统一 UI |
| 岗位中心 | 职位池、导入/同步、筛选、对比、收藏和详情 | 保留并统一命名 |
| JD 分析 | 技能提取、证据匹配、缺口与准备任务 | 增强知识联动 |
| 知识空间 | Markdown、属性、标签、双向链接、反向链接和关系图 | 已升级 |
| 项目资产库 | 项目介绍、面试问题、故障案例和学习知识 | 已新增聚合入口 |
| 求职管道 | 机会阶段、沟通草稿、下一步与截止时间 | 保留 |
| 公司关注 | 招聘官网、岗位变化和招聘时间线 | 保留 |
| 能力成长 | 已验证能力、相关经验、缺口和补强路线 | 保留并统一命名 |
| 数据中心 | 采集、清洗、标准化、质量、日志和导出 | 保留 |
| 设置 | 本地数据、备份、AI Provider 和兼容导出 | 保留 |

v0.5.0 的求职 Agent、岗位洞察、职业档案、求职日程和训练报告仍可从工作台或业务页面进入，路由与数据未删除。

## 3. 工作台

首页从“大量数据总览”调整为行动优先：

1. 今日任务：汇总求职管道下一步和知识复习任务。
2. 今日训练：显示当天职业教练会话数量。
3. 待优化回答：集中展示待复习的面试回答。
4. 待学习技能：读取能力图谱的真实缺口。
5. 目标岗位：显示未忽略、未关闭的高匹配岗位。

工作台同时保留职业档案、求职 Agent、岗位洞察、公司关注、简历工坊和 JD 分析等辅助入口。

## 4. AI 职业教练

AI 助手与面试训练统一到一个入口，但旧 `/assistant` 和 `/training` 路由继续重定向到 `/coach`。

支持六种模式：

- 模拟面试：按目标岗位出题、回答、追问、评分和复盘。
- 项目深挖：围绕架构、故障、优化和个人贡献追问。
- 技术问答：覆盖 Kubernetes、Docker、云计算、数据库与网络等主题。
- 简历追问：必须关联一个简历版本，验证核心表述的真实证据。
- JD 分析：围绕技能要求、面试重点和学习建议训练。
- 英语面试：问题、推荐回答、反馈、追问和总结均保持英文。

统一会话使用 `CoachSession`，关联目标岗位、简历和多个项目，并保存消息、回答和训练报告。旧 `TrainingSession` 继续保留，既有报告页和历史记录不受影响。

训练完成后自动生成知识卡，包含：

- 原回答；
- 优化回答；
- 证据与表达不足；
- 改进方案；
- 次日复习任务。

## 5. 知识空间

知识空间成为应用内置知识体系，Obsidian 降为可选兼容导出。

### 5.1 内容能力

- Markdown 标题、列表、代码块、表格、图片与链接；
- 标签、类型、状态、可见性、岗位、项目、技能、来源和复习时间；
- `[[知识标题]]` 双向链接语法；
- 当前知识的反向链接；
- 知识、项目、岗位、技能之间的关系图。

### 5.2 业务联动

- 保存项目时，稳定生成项目介绍、面试问题、故障与风险、学习知识四类知识卡；
- 分析 JD 时，生成技能要求和准备任务知识卡；
- 完成训练时，生成结构化回答复盘知识卡；
- 删除知识时，清理项目和知识中的相关引用。

### 5.3 数据可见性

每条知识包含 `private`、`publish-ready` 或 `public` 状态。v0.6.0 默认 `private`；本版本不把私有内容自动发布到网络。

## 6. 设计系统

新增 `src/design-system` 和 `components/ui`，为旧页面提供兼容变量并为新页面提供语义组件。

| 角色 | 规范 |
|---|---|
| 页面标题 | 24px |
| 模块标题 | 18px |
| 卡片标题 | 16px |
| 正文 | 14px |
| 辅助文字 | 12px |

中文字体优先 `Microsoft YaHei UI`、`PingFang SC`；英文优先 `Inter`；代码优先 `JetBrains Mono`。间距使用 4、8、12、16、20、24、32、40、48px，页面左右 24px，卡片 20px，模块间 24px。

统一组件包括 Button、Card、Input、Select、Modal、Table、Tag、Badge、Empty、Loading、PageHeader 和 MarkdownEditor。所有交互控件保留可见键盘焦点、禁用状态和加载反馈，并支持 `prefers-reduced-motion`。

## 7. 数据与迁移

工作区 schema 从 2 升级到 3。迁移覆盖知识属性、统一教练会话和迁移记录，不删除 v0.5.0 字段。

迁移顺序：

1. 读取 `database/state.json` 的原始 schema 版本；
2. 验证并规范化为 schema 3；
3. 在 `backups/` 保存迁移前工作区副本；
4. 在 `migrations/` 写入迁移报告；
5. 原子写入新状态；
6. 当前文件无效时尝试 `state.previous.json` 恢复。

旧训练记录会转换为对应的 `CoachSession`，问题和回答按消息序列保留；原 `trainingSessions` 仍然存在。旧知识自动补齐岗位、项目、技能关联数组和 `private` 可见性。

## 8. 技术架构

- 桌面壳：Electron 37，启用上下文隔离和安全 IPC。
- 前端：Vue 3、TypeScript、Vue Router、Vite、Lucide。
- 主进程：服务层、连接器层、文档导入和原子工作区仓库。
- 数据：本地 JSON 工作区、previous 恢复文件、显式备份、迁移记录和 Markdown 导出。
- AI：OpenAI 兼容与 Dify Provider；未配置时使用本地规则降级。
- 岗位：浏览器可见岗位 Bridge 与数据源契约；不绕过登录、验证码或平台风控。

## 9. 验收结果

截至 2026-07-22：

- TypeScript/Vue 类型检查通过；
- 16 个测试文件、60 项单元与集成测试通过；
- Vite 与 Electron 生产构建通过；
- Electron 主流程 E2E 通过；
- 12 个主模块的 12/14/16/18/24px 统一字号、24px 页面边距与横向溢出 E2E 通过；
- 12 个主模块的无通知遮挡截图已保存到 `docs/screenshots/v0.6.0`；
- Windows 安装版与便携版尚未生成，因此打包产物冒烟测试仍为待执行。

后续路线为 v0.7.0 岗位连接、v0.8.0 知识社区、v1.0.0 完整职业 AI Agent。任何外部抓取、消息发送或岗位投递都必须独立授权、遵守平台规则，并保留用户确认与审计记录。
