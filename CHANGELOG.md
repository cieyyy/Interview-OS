# Changelog

## 0.4.0 - 2026-07-21

### Added

- 新增基于目标 JD、职业档案和项目经历的压力面试闭环，可选择 2、4、6 或 8 轮训练。
- 每轮根据上一轮回答动态追问，并对已问问题进行归一化去重，避免反复追问同一内容。
- 每次回答新增证据缺口、逻辑与表达问题、面试官可能质疑点、事实约束 STAR 回答和简历修改判断。
- 简历修改建议可一键同步到对应项目经历的“面试校准记录”，形成训练和简历修正闭环。
- 压力训练完成后输出核心优势、三个高风险漏洞、五个练习问题、简历修改建议和面试前检查清单。
- 训练报告支持回看每轮诊断、动态追问依据、简历同步建议和最终压力面试总结。

### Changed

- 压力模式由固定题目列表改为一次只问一题，回答完成后再生成下一轮问题。
- AI Provider 请求现在会携带目标公司、目标岗位、JD、项目、档案和历史轮次，并明确禁止编造经历或数据。
- 未配置或调用 AI Provider 失败时，继续使用本地规则生成诊断、动态追问和闭环总结。

### Known limitations

- 本地规则能识别常见证据缺口，但复杂行业事实仍需用户核对；无法确认的信息会标记为“【需要本人补充】”。
- 在线追问与总结质量取决于已配置模型及 OpenAI 兼容中转站的结构化输出能力。
- Windows 构建产物尚未使用商业代码签名证书，首次运行前请核对 SHA-256。

## 0.3.0 - 2026-07-19

### Added

- 完整简历可识别一个或多个项目经历，并按项目名称去重后自动保存；
- 简历导入新增旧版 DOC、RTF/MHTML 伪扩展名、表格制表符、双栏 PDF 和中英文日期项目的适配；
- 扫描型 PDF 可在用户启用 AI Provider 后识别前 5 页；项目名称丢失时保留为可编辑的“待命名项目”；
- 已记录项目支持回填表单、编辑和保存修改；
- 面试训练支持中文和 English 两种语言；
- 面试训练新增麦克风语音识别、题目朗读、AI 1V1 陪练和推荐回答；
- AI Provider 未配置或请求失败时，陪练自动降级到本地规则教练；
- 推荐回答改为直接显示在答题区右侧，不再生成在首屏下方；Provider 测试改为验证一次真实模型生成请求；
- 训练历史支持点击查看问题、每次回答、六维分数和改进建议；
- Electron 麦克风权限限制为当前应用窗口的音频请求，摄像头请求保持拒绝。

### Changed

- “项目证据”统一更名为“项目经历”，“已沉淀项目”更名为“已记录项目”；
- 设置页改为每行一个功能卡片；
- AI 助手页精简为单一“进入训练”入口；
- 顶部数据提示改成可理解的本地优先与主动发送说明。

### Known limitations

- Chromium Web Speech Recognition 的可用性取决于 Windows 语音组件、麦克风权限和网络环境；不可用时仍可文字作答；
- 纯图片、艺术字、复杂浮动文本框或缺少项目名称的简历仍需要人工核对；
- Windows 安装包尚未使用商业代码签名证书，首次运行前请核对 SHA-256。

## 0.2.0 - 2026-07-19

### Added

- JD 中心支持上传图片或文件，识别后预填岗位、公司和 JD 原文；
- 职业档案支持上传简历图片或文件，识别后预填基本档案；
- 面试知识库支持上传图片或文件，识别后生成可编辑的知识卡片草稿；
- 本地解析 TXT、Markdown、JSON、CSV、LOG、DOCX 和文本型 PDF；
- 图片识别支持 OpenAI 兼容的 Chat Completions，并自动回退到 Responses API，兼容常见 Sub2API 中转配置。

### Changed

- 移除 Windows 桌面窗口的原生菜单栏；
- 所有导入结果只预填、不自动保存，必须由用户检查确认；
- 单文件大小限制为 12 MB，文件访问仅限用户在系统选择器中主动选择的文件。

### Known limitations

- 扫描版 PDF 暂不直接 OCR，可将页面导出为 PNG/JPG 后通过图片识别；
- 图片识别需要启用具备视觉能力的 AI Provider，内容会发送给该 Provider；
- DOCX 中的复杂排版与内嵌图片不会完整还原。

## 0.1.0 - 2026-07-19

### Added

- 本地优先的职业档案、项目经历和面试知识库；
- JD 离线分析、能力要求提取和证据匹配；
- 面试问题生成、六维评分、重答和知识沉淀；
- 训练报告、原子数据保存、备份和 Markdown 导出；
- OpenAI 兼容接口和远程 Dify Provider 抽象；
- Electron 安全 IPC、上下文隔离和密钥安全存储；
- Windows 安装版和便携版；
- 单元、集成、Electron E2E 和打包产物冒烟测试。

### Known limitations

- 未签名测试发行版；
- 暂无云同步、自动更新、招聘网站连接器和语音面试；
- 暂用 Electron 默认应用图标。
