# Interview OS v0.6.0 界面截图

本目录包含 12 个一级功能模块的最终界面截图和 `audit.json` 自动审计结果。

统一验收条件：

- 页面左右内边距 24px；
- 页面标题区下间距 24px；
- 可见文字仅使用 12/14/16/18/24px 设计字号；
- 主内容区无横向溢出；
- 一级导航与当前模块保持高亮一致。

28 个子功能交互状态由 `tests/e2e/subviews.spec.ts` 逐项点击验证，运行时截图输出到 `artifacts/subviews`。该覆盖同时检查父级导航高亮、表单卡片边距和子页面字号。
