# Interview OS 0.1.0 测试报告

## 1. 测试结论

MVP 核心闭环通过，达到本地试用和继续迭代标准。安装版与便携版均已生成；已打包程序在 Windows 10 21H1 上完成启动和本地写入冒烟测试。

## 2. 环境

- 操作系统：Windows 10 21H1，Build 19043；
- 架构：x64；
- Electron：37.10.3；
- Vue：3.5；
- TypeScript：5.8；
- 数据：隔离临时目录与 F 盘项目目录；
- Docker / WSL / BIOS 虚拟化：不依赖。

## 3. 自动化结果

| 测试项 | 结果 | 证据 |
|---|---|---|
| TypeScript / Vue 类型检查 | 通过 | 0 错误 |
| 单元与集成测试 | 通过 | 5 个文件，12 个用例 |
| Renderer 生产构建 | 通过 | 48 个模块完成打包 |
| Electron 完整 E2E | 通过 | 1 个完整用户闭环 |
| Windows 打包产物冒烟 | 通过 | 1 个正式构建启动与写入测试 |
| 生产依赖漏洞检查 | 通过 | 0 个已报告漏洞 |
| 凭据模式扫描 | 通过 | 未发现疑似真实 API Key |

## 4. 已验证的用户闭环

1. 应用首次启动并显示空工作台；
2. 加载演示职业档案和项目数据；
3. 新建知识卡片并立即显示；
4. 导入目标 JD 并完成离线结构化分析；
5. 选择 JD 和项目开始面试训练；
6. 提交回答并生成分项评分；
7. 保存最终回答到知识库；
8. 关闭并重启应用；
9. 验证知识、JD 和训练数据仍然存在；
10. 启动打包后的 `win-unpacked` 正式程序并完成隔离数据写入。

## 5. 修复记录

- 修复 Vue 响应式 Proxy 直接传入 Electron IPC 时出现 `An object could not be cloned`；
- 将 IPC 输入统一转换为纯 JSON 数据；
- 将 preload 改为独立运行，避免沙箱预加载脚本依赖应用模块；
- 增加 Renderer 退出和加载失败诊断；
- 修复 Electron E2E 关闭后再次读取进程对象导致的测试清理异常；
- 修复安装版与便携版产物同名覆盖问题。

## 6. 发布产物

| 产物 | 大小 | SHA-256 |
|---|---:|---|
| `Interview-OS-0.1.0-x64-portable.exe` | 87.57 MB | `9BAA9D2562D60B2380648EE2AC6BD405359DB6E8A22D074CD6FC1248F3A72208` |
| `Interview-OS-0.1.0-x64-setup.exe` | 87.79 MB | `C220B87F4B7EB06A1D2165A1EFBDA92CE621E188C610C209B25C1AB02113F2C8` |

## 7. 已知限制

- 当前发布文件未使用商业代码签名证书，Windows 可能显示未知发布者提示；
- AI Provider 已实现配置、密钥隔离和连接测试接口，但未使用真实付费 Key 做在线内容生成验收；
- 当前无云同步、自动更新、招聘网站自动抓取和团队协作；
- 当前使用 Electron 默认图标，正式公开发布前应补充品牌图标；
- Electron 测试在 Codex 受限进程沙箱内无法创建 Renderer，需在已授权的项目测试进程中执行；正式打包程序本身已验证可运行。

## 8. 风险判断

- 未发现 P0 / P1 功能缺陷；
- 本地数据原子保存、上一版本恢复、备份和 Markdown 导出已具备；
- 默认不启用联网 Provider，离线功能不依赖外部服务；
- 项目总占用约 1.37 GB，其中包含开发依赖、Electron 打包缓存和两种发布产物，不会持续无上限增长。
